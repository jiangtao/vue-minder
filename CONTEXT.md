# Vue Minder

Vue Minder 将 KityMinder Core 封装为适配不同 Vue 主版本的脑图组件，并提供可公开验证的演示站点。

## Language

**版本维护线（Maintenance Line）**：
与一个 Vue 主版本对应、独立发布和维护的 Git 分支。本项目使用 `3.x` 对应 Vue 3、`2.x` 对应 Vue 2，历史 `master` 保留为 Vue 1。
_Avoid_: 当前版本、主分支版本

**演示站点（Demo Site）**：
由当前默认版本维护线构建并部署的浏览器示例，用于体验和验证 Vue Minder 的公开能力。
_Avoid_: 测试页、主页
